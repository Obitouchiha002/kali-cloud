// When the Kali engine (server + Docker) is live on a VPS, set its URL here,
// e.g. window.KALI_ENGINE_URL = "https://app.your-domain.com";
// Empty = engine not deployed yet (Launch shows "coming soon" on static hosts).
window.KALI_ENGINE_URL = "";
